function underscore(string) {
    // create empty guess string 
    for (var i = 0; i < string.length; i++) {
        // if alpha
            // push "_"
        // else 
            // push char
    }
    // return underscore
}

function game(string) {
    // print underscore(string); 
    // prompt user for a letter
    for (var i = 0; i < string.length; i++) {
        // if indexOf guess === -11
            // wrong answer
        // else if 
    }
}